feign:
  client:
    config:
      default:
        connectTimeout: 10000
        readTimeout: 600000
        loggerLevel: full
  logger:
    audit:
      loggerLevel: full
    store:
      loggerLevel: full
