sportx:
  global: local>application2



feign:
  client:
    config:
      default:
        connectTimeout: 100000
        readTimeout: 600000
        loggerLevel: BASIC
  logger:
    audit:
      loggerLevel: BASIC
    store:
      loggerLevel: BASIC
